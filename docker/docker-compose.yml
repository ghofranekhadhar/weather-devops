version: '3.8'
services:
  backend:
    build: ../backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - OPENWEATHER_API_KEY=${OPENWEATHER_API_KEY}

  frontend:
    build: ../frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
